JSON Compare

Front-end (React + Vite + Tailwind) calls local backend `/compare` endpoint implemented in Go.

Run backend:

    cd /Users/outsider/Downloads/Golang_Development/json_compare
    go run main.go

Run frontend:

    cd frontend
    npm install
    npm run dev

Open http://localhost:5173 and http://localhost:8080 (backend)
